name: Apply approved UI

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply approved Home and Companies source
        shell: python
        run: |
          print('Manual-only workflow. No automatic source rewrite is performed.')
